# 依赖
node_modules/

# 运行时数据（SQLite 数据库、敏感 token、上传文件）
data/*.db
data/*.db-shm
data/*.db-wal
data/.yly_token.json
public/uploads/

# 环境变量（保留 .env.example 模板）
.env
.env.local

# 系统文件
.DS_Store
*.log

# 本地 SSH 密钥与授权文件（生产由安装脚本/后端运行时生成）
server/data/ssh-keys/
server/data/authorized_keys
